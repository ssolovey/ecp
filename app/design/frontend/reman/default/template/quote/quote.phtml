<div id="steps">
    <!-- 
        First Step Quick quote (MAKE DATA)
    -->
    <span class="steps_lable">Make:</span>
   <!-- <select class="select_box" id="qq_select_make" onchange="Reman.QuickQuoteModule.selectMake(this.selectedOptions[0].getAttribute('startyear'),this.selectedOptions[0].getAttribute('endyear'),this.value)">-->
         <select class="select_box" id="qq_select_make" onchange="selectMake()">
		 <option selected="selected">...</option>
         <?php  foreach($this->getMake() as $value): ?>
                <option value="<?php echo $value['make_id']?>" startyear="<?php echo $value['start_year']?>" endyear="<?php echo $value['end_year']?>">
                    <?php echo $value['make'] ?>
                </option>
         <?php endforeach; ?>
    </select>
    
    <!-- 
        Second Step Quick quote (Select Year)
    -->
    <span class="steps_lable">Year:</span>
    <select class="select_box" id="qq_select_year"  onchange="selectModel(this.value)">
         <option value="none" selected="selected">...</option>
    </select> 
    
    <!-- 
        Third Step Quick quote (Select Model)
    -->
    <span class="steps_lable">Model:</span>
    <select class="select_box" onchange="selectProductID(this.value)" id="qq_select_model">
         <option selected="selected">...</option>
    </select> 

</div>
<!-- 
	Forth Step Quick quote (Select Part Number)
-->

<div id="select_part_cont">
</div>

<div id="info">
	<ul class="info_list">
		<li class='label'>INFO:</li>
	</ul>

</div>